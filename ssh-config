# Machine-specific and private hosts live in ~/.ssh/config.local, which is not
# tracked here. A missing include is not an error, so this is safe on a fresh box.
Include ~/.ssh/config.local

Include ~/.orbstack/ssh/config

Host *
  UseKeychain yes
  AddKeysToAgent yes
  IdentityFile ~/.ssh/id_rsa
